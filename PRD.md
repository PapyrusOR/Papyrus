# Papyrus 产品需求文档 (PRD)

> **版本**: v1.2.2 抽象版  
> **定位**: UI 框架无关、平台无关的原始需求文档  
> **适用范围**: 任何技术栈的重构实现（桌面端、移动端、Web、TUI 等）

---

## 1. 产品概述

Papyrus 是一款以卡片（Flashcard）为知识载体的间隔重复（Spaced Repetition）学习工具，内置 AI 学习助手。核心设计哲学为：**极简、键盘驱动、流状态（Flow State）优先**。

### 1.1 核心定位
- **学习引擎**: 基于 SM-2 算法的科学记忆调度系统。
- **知识库**: 用户可手动创建或批量导入卡片，形成个人知识库。
- **AI 副驾驶**: 在学习过程中提供实时提示、解释、扩展和相关练习生成，并可主动操作卡片数据。

### 1.2 设计原则
1. **无鼠标操作**: 核心学习流程应完全可通过键盘完成。
2. **本地优先**: 用户数据默认存储在本地，不上传云端（除非用户主动配置云端 AI 提供商）。
3. **容错与降级**: AI 模块为可选依赖；缺失时主学习功能必须正常运行。
4. **隐私保护**: API 密钥等敏感信息在界面上应脱敏显示，配置中需做合法性校验。

---

## 2. 核心概念与术语

| 术语 | 定义 |
|------|------|
| **卡片 (Card)** | 知识的最小单元，包含题目（Question）和答案（Answer），以及算法状态字段。 |
| **卷头 / 卷尾** | 产品的隐喻术语：卷头 = 题目面；卷尾 = 答案面。 |
| **到期卡片 (Due Cards)** | 当前时间已超过 `next_review` 时间戳的卡片集合。 |
| **复习会话 (Review Session)** | 用户连续完成多张到期卡片评分的一次学习过程。 |
| **评分 (Grade)** | 用户对记忆质量的自我评估，三档：忘记(1)、模糊(2)、秒杀(3)。 |
| **AI 会话 (Chat Session)** | AI 助手的独立对话上下文，支持多会话并存、切换、重命名。 |
| **Agent 模式** | AI 可在对话中调用工具（增删改查卡片），直接操作数据。 |
| **Chat 模式** | AI 仅做纯对话回复，不附加工具定义，不执行操作。 |
| **MCP** | 本地暴露的轻量 HTTP 服务，将卡片操作能力以标准化接口提供给外部。 |

---

## 3. 数据模型

### 3.1 卡片 (Card)

```json
{
  "q": "string, required",           // 题目/问题
  "a": "string, required",           // 答案/解析
  "next_review": 0,                   // 下次复习时间戳（秒级 Unix Time），0 表示立即复习
  "interval": 0,                      // 当前间隔（秒），由 SM-2 算法维护
  "ef": 2.5,                          // Easiness Factor，难度系数，默认 2.5
  "repetitions": 0,                   // 连续正确次数
  "tags": ["string"]                  // 可选：标签列表
}
```

**向后兼容要求**: 旧数据可能缺少 `ef` 和 `repetitions` 字段，读取时应以默认值（`ef=2.5`, `repetitions=0`）自动补齐，无需迁移脚本。

### 3.2 AI 配置 (AI Config)

```json
{
  "providers": {
    "openai": {
      "api_key": "string",
      "base_url": "https://api.openai.com/v1",
      "models": ["gpt-4", "gpt-3.5-turbo"]
    },
    "anthropic": {
      "api_key": "string",
      "base_url": "https://api.anthropic.com/v1",
      "models": ["claude-3-opus-20240229"]
    },
    "ollama": {
      "base_url": "http://localhost:11434",
      "models": ["llama2", "mistral"]
    },
    "custom": {
      "api_key": "string",
      "base_url": "string",
      "models": []
    }
  },
  "current_provider": "string",
  "current_model": "string",
  "parameters": {
    "temperature": 0.7,
    "top_p": 0.9,
    "max_tokens": 2000,
    "presence_penalty": 0.0,
    "frequency_penalty": 0.0
  },
  "features": {
    "auto_hint": false,
    "auto_explain": false,
    "context_length": 10
  }
}
```

### 3.3 AI 会话 (Session)

```json
{
  "active_session_id": "string",
  "sessions": [
    {
      "id": "string",
      "title": "string",
      "messages": [
        {
          "role": "user | assistant",
          "content": "string",
          "attachments": [
            {
              "id": "string",
              "name": "string",
              "stored_name": "string",
              "path": "string",
              "type": "image | document",
              "mime_type": "string",
              "size": 0,
              "created_at": 0
            }
          ]
        }
      ],
      "created_at": 0,
      "updated_at": 0
    }
  ]
}
```

**会话约束**:
- 必须始终保留至少一个会话。
- 删除会话时，若删除的是当前活跃会话，应自动将活跃会话切换至剩余会话中的某一个。
- 按 `updated_at` 倒序排列会话列表。

### 3.4 日志分类

| 日志类型 | 用途 | 格式 |
|---------|------|------|
| 主日志 (main) | 常规运行日志，DEBUG 及以上级别 | SQLite `log_entries` 表 |
| 错误日志 (error) | 仅 ERROR 级别及以上 | SQLite `log_entries` 表 |
| 活动日志 (activity) | 用户行为追踪（如评分、添加卡片） | SQLite `log_entries` 表 |
| 事件日志 (events) | 结构化事件，如 AI 工具调用、MCP 请求 | SQLite `log_entries` 表 |

**脱敏要求**: 日志中若出现键名包含 `api_key`、`authorization`、`token`、`secret`、`password`、`key` 的字段，值必须做掩码处理（如前3字符 + `***` + 后2字符）；字符串长度超过阈值（如 800 字符）应截断并标注原始长度。

---

## 4. 功能需求

### 4.1 卡片管理

#### 4.1.1 添加单张卡片
- 提供表单输入题目和答案。
- 两者均为必填；为空时应阻止保存并提示用户。
- 保存后自动持久化，新卡片的 `next_review` 初始为 `0`（立即可复习）。

#### 4.1.2 批量导入
- 支持从 UTF-8 编码的文本文件导入。
- 文件格式：以空行分隔的块，每块内使用 `===` 分割题目和答案。
- 示例：
  ```
  题目 A === 答案 A

  题目 B === 答案 B
  ```
- 导入完成后应展示导入结果摘要（成功数量、内容预览）。

#### 4.1.3 删除卡片
- 可删除当前正在学习的卡片。
- 删除前应要求用户二次确认。

#### 4.1.4 搜索卡片
- 支持按关键词全文搜索（同时匹配题目和答案，不区分大小写）。
- 返回结果应包含卡片索引、题目、答案摘要（超长时截断）。

---

### 4.2 学习引擎（SM-2 间隔重复）

#### 4.2.1 复习调度
- 系统维护一个全局卡片列表。
- 每次进入下一张卡片时，筛选出 `next_review <= 当前时间` 的卡片作为"到期卡片"。
- 若无到期卡片，显示完成提示（如"今日任务已完成"），并周期性（如每 5 秒）自动检查是否有新到期卡片。

#### 4.2.2 学习界面状态机
学习界面包含两种互斥状态：

1. **题目状态**: 仅显示当前卡片的题目（卷头）。
   - 提供触发器（如按键/按钮）以进入答案状态。
   
2. **答案状态**: 同时显示题目和答案（卷头 + 卷尾）。
   - 提供三个评分选项：忘记(1)、模糊(2)、秒杀(3)。
   - 从进入答案状态到允许评分，应有最小防抖时间（如 0.5 秒），防止误触。

#### 4.2.3 SM-2 算法规则

将用户三档评分映射为 SM-2 质量分（Quality, 0-5）：
- 忘记(1) → Quality = 1
- 模糊(2) → Quality = 3
- 秒杀(3) → Quality = 5

**算法步骤**:
1. **判断正确性**:
   - 若 Quality >= 3（模糊/秒杀视为正确）:
     - `repetitions == 0` → 间隔 = 1 天
     - `repetitions == 1` → 间隔 = 6 天
     - `repetitions >= 2` → 间隔 = 上次的间隔天数 × `ef`
     - `repetitions += 1`
   - 若 Quality < 3（忘记）:
     - `repetitions = 0`
     - 间隔 = 1 天

2. **更新 Easiness Factor**:
   - 公式: `ef = ef + (0.1 - (5 - quality) × (0.08 + (5 - quality) × 0.02))`
   - 下限约束: `ef` 不得低于 `1.3`。

3. **持久化**:
   - `next_review = 当前时间 + 间隔(秒)`
   - `interval = 间隔(秒)`
   - 保存 `ef`（保留两位小数）和 `repetitions`。

#### 4.2.4 状态栏信息
- 持续显示：当前到期卡片数量、总卡片数量。

---

### 4.3 AI 助手

AI 助手以独立的辅助面板形式存在，与主学习区并列。AI 功能为可选模块；未启用时应显示友好的降级提示，告知用户如何安装可选依赖。

#### 4.3.1 提供商抽象
系统应支持以下四类提供商，且可通过统一接口调用：

| 类型 | 认证方式 | 模型列表获取 | 协议特点 |
|------|---------|------------|---------|
| OpenAI 兼容 | Bearer Token | `/models` GET | 标准 Chat Completions API |
| Anthropic | Bearer Token | `/models` GET | 兼容 OpenAI API 格式 |
| Ollama | 无认证 | `/api/tags` GET | 本地运行，自定义 `/api/chat` 端点 |
| 自定义 | 可选 Token | `/models` GET | 兼容 OpenAI API 格式 |

**通用要求**:
- API Key 和 Base URL 在保存前应做 ASCII 合法性校验，拒绝包含非 ASCII 字符（如中文、特殊空格）的配置。
- 请求超时：常规请求 60 秒，Ollama 本地请求可放宽至 120 秒。
- 网络异常时应给出明确的本地化错误提示。

#### 4.3.2 模型管理
- 每个提供商维护独立的模型名称列表。
- 用户可手动添加、编辑、删除模型名称。
- 切换提供商时，若当前选中的模型不在新提供商的列表中，应自动回退至该提供商的第一个可用模型，并提示用户。
- 每个提供商至少保留一个模型；禁止删除最后一个模型。

#### 4.3.3 对话功能

**消息发送**:
- 输入框支持发送文本消息。
- 支持快捷键发送（如 Enter），同时支持换行快捷键（如 Shift+Enter）。
- 发送时应展示"思考中/加载中"状态指示。

**上下文管理**:
- 系统应自动在每条消息中注入当前正在学习的卡片上下文（题目、答案、当前处于题目/答案状态）。
- 支持按配置的历史轮数（`context_length`）保留对话上下文。
- 上下文中的用户历史消息若包含附件，应按提供商格式正确序列化。

**附件支持**:
- 支持单次上传最多 5 个文件。
- 支持文件类型：图片（PNG/JPG/JPEG/WEBP/GIF）、文档（PDF/TXT/MD/DOCX）。
- 单文件大小上限 10MB。
- 支持文件选择对话框和拖拽上传两种方式。
- 附件应存储在会话隔离的目录中，按 UUID 重命名以避免冲突。
- 发送给 OpenAI 兼容模型时：
  - 图片转为 Base64 Data URL，嵌入为 `image_url` 类型内容块。
  - TXT/MD 文件读取文本内容作为附加文本块。
  - 其他文档类型标记为"已上传但未解析"。
- 发送给非 OpenAI 模型时：降级为纯文本附加说明（列出文件名及 TXT/MD 的内容摘要）。

#### 4.3.4 模式切换
- **Agent 模式**: 系统提示词（System Prompt）中附加工具定义（见 4.3.5）。AI 回复后，系统应自动解析回复中是否包含工具调用 JSON 代码块，若包含则执行对应工具，并将执行结果以系统消息形式追加到对话中。
- **Chat 模式**: 不附加工具定义，AI 仅做纯文本回复。

#### 4.3.5 AI 工具定义
Agent 模式下，AI 可通过特定 JSON 格式调用以下工具：

| 工具名 | 功能 | 参数 |
|-------|------|------|
| `create_card` | 创建新卡片 | `question`, `answer`, `tags` |
| `update_card` | 更新指定卡片 | `card_index`, `question?`, `answer?` |
| `delete_card` | 删除指定卡片 | `card_index` |
| `search_cards` | 搜索卡片 | `keyword` |
| `get_card_stats` | 获取学习统计 | 无 |
| `generate_practice_set` | 按主题生成练习题 | `topic`, `count`（预留，可返回未实现） |

**工具调用格式**:
```json
{\n  "tool": "create_card",\n  "params": {\n    "question": "...",\n    "answer": "..."\n  }\n}
```

**执行规则**:
- 所有修改类工具调用应立即执行并持久化到数据存储。
- 执行完成后，主学习区应自动刷新（如加载下一张到期卡片）。
- 执行结果（成功/失败/错误信息）应反馈到 AI 对话面板中。

#### 4.3.6 预设 AI 能力
除自由对话外，系统还应支持以下定向能力（可通过快捷入口或自然语言触发）：
- **智能提示**: 基于当前题目给出不透露答案的启发性提示。
- **答案解释**: 用通俗语言解释当前卡片的答案。
- **生成相关题**: 基于当前知识点生成 3 个相关的拓展问题。
- **错题分析**: 分析用户近期答错的知识点，给出学习建议。

---

### 4.4 会话管理

- 每个 AI 对话上下文对应一个独立的"会话"。
- 会话列表应显示标题、消息数量、最近更新时间。
- 操作：
  - **新建**: 创建空消息列表的新会话，并自动切换至该会话。
  - **切换**: 切换活跃会话，重新渲染对应的历史消息。
  - **重命名**: 修改会话标题，不能为空。
  - **删除**: 删除非最后一个会话。
- "清空对话"语义上等同于"新建会话"（保留旧会话历史，开启新上下文），而非删除历史。

---

### 4.5 设置与配置

设置界面应包含以下配置维度：

1. **提供商管理**:
   - 选择当前活跃提供商（OpenAI / Anthropic / Ollama / Custom）。
   - 配置各提供商的 API Key（输入框脱敏显示）和 Base URL。

2. **模型管理**:
   - 查看当前提供商下的模型列表。
   - 手动添加/编辑/删除模型名称。
   - 从提供商 API 自动拉取模型列表（若支持）。

3. **生成参数**:
   - Temperature（范围 0~2，步进 0.1）
   - Max Tokens（范围 100~4000，步进 100）
   - 其他可选参数：top_p、presence_penalty、frequency_penalty

4. **保存策略**:
   - 保存前执行全量配置验证（ASCII 校验、模型兼容性校验）。
   - 验证失败时应阻止保存，并明确提示出错的字段。
   - 保存后立即持久化到配置文件。

---

### 4.6 数据持久化与备份

#### 4.6.1 数据存储
- 所有数据统一存储在嵌入式 SQLite 数据库（通过 Drift ORM 管理）。
- 数据库文件为单文件 `papyrus_db.sqlite`，位于应用私有数据目录。
- 表结构：
  - `cards` — 卡片数据（id, q, a, next_review, interval, ef, repetitions, tags, created_at）
  - `ai_providers` / `ai_provider_models` / `ai_settings` — AI 配置（规范化存储）
  - `sessions` / `messages` / `attachments` / `active_session` — AI 会话（关系型存储）
  - `log_entries` — 日志条目（timestamp, level, category, message, metadata）
- 所有写入通过 Drift 的事务机制保证原子性，无需手动文件原子替换。

#### 4.6.2 自动备份
- 策略：在每次数据写入（保存）时，若距离上次备份已超过 1 小时，则自动将当前 SQLite 数据库文件复制到备份目录。
- 备份以目录形式存储（`auto_YYYYMMDD_HHMMSS/` 或 `manual_YYYYMMDD_HHMMSS/`），内含 `papyrus_db.sqlite`。

#### 4.6.3 手动备份与恢复
- 用户可一键创建即时备份。
- 用户可从备份文件恢复数据；恢复前应要求确认（当前数据将被覆盖）。
- 危险操作（如重置所有数据）执行前，应强制创建一次备份。

#### 4.6.4 数据重置
- 提供清空所有卡片数据的功能。
- 操作前必须二次确认，并建议先备份。

---

### 4.7 日志与诊断

#### 4.7.1 日志记录
- 系统启动/退出、用户关键操作（评分、添加/删除卡片、导入、备份/恢复、AI 请求、工具调用）均应记录。
- 支持分级：DEBUG、INFO、WARNING、ERROR。
- 支持分类存储（主日志、错误日志、活动日志、事件日志）。

#### 4.7.2 日志查看器
- 提供独立的日志浏览界面。
- 支持按日志类型筛选（全部/错误/活动/事件）。
- 支持按行数限制加载（如 50/100/200/500/全部）。
- 支持关键词实时搜索过滤。
- 支持一键刷新。
- 支持导出所有日志为单一文本文件（带时间戳命名）。
- 支持清空所有日志（需二次确认）。
- 日志文本应按级别高亮显示（如 INFO 绿色、WARNING 黄色、ERROR 红色）。

---

### 4.8 MCP 服务接口

系统应可选地在后台启动一个轻量级 HTTP 服务器，将卡片操作能力暴露给外部客户端（如编辑器插件、其他 AI 工具）。

#### 4.8.1 服务端点

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查，返回 `{"status": "ok"}` |
| GET | `/tools` | 返回可用工具列表 |
| POST | `/call` | JSON-RPC 风格调用，执行指定工具 |

#### 4.8.2 POST /call 请求格式
```json
{
  "tool": "search_cards",
  "params": { "keyword": "递归" }
}
```

#### 4.8.3 响应格式
直接返回工具执行结果 JSON，与内部 `CardTools.execute_tool()` 的输出一致。

#### 4.8.4 运行要求
- 默认绑定本地回环地址（127.0.0.1），避免外部网络暴露。
- 支持跨域（CORS），允许浏览器端调用。
- 在独立后台线程运行，不阻塞主程序。
- 程序退出时应正确关闭服务器。

---

## 5. 用户交互流程

### 5.1 核心学习流程（键盘驱动）

```
[启动] → 加载数据 → 显示第一张到期卡片（仅题目）
         ↓
    用户按"显示答案" → 展开答案面
         ↓
    用户按 1/2/3 评分
         ↓
    SM-2 算法更新卡片间隔 → 保存数据
         ↓
    加载下一张到期卡片
         ↓
    若无到期卡片 → 显示完成状态 → 定时轮询检查
```

**快捷键映射（示例，可按平台习惯调整）**:
- 显示答案: Space
- 评分-忘记: 1
- 评分-模糊: 2
- 评分-秒杀: 3
- 全局搜索: Ctrl/Cmd + K（可选扩展）

### 5.2 AI 对话流程

```
[用户输入消息] → 组装系统提示词（含当前卡片上下文 + 可选工具定义）
         ↓
    附加历史上下文（最近 N 轮）
         ↓
    调用当前提供商 API
         ↓
    流式/非流式接收回复 → 展示 AI 消息
         ↓
    若 Agent 模式 → 解析工具调用 → 执行 → 展示执行结果
```

### 5.3 设置配置流程

```
[打开设置] → 加载当前配置
         ↓
    用户修改提供商/Key/URL/模型/参数
         ↓
    [点击保存] → 执行验证（ASCII、模型兼容性）
         ↓
    验证通过 → 写入配置文件 → 同步所有相关 UI 状态
         ↓
    验证失败 → 阻止保存，展示具体错误字段
```

---

## 6. 非功能需求

### 6.1 性能
- 卡片数据加载应在毫秒级完成（假设常规用户数据量 < 10,000 张卡片）。
- AI 请求应在独立线程/异步任务中执行，不得阻塞主界面交互。
- 日志写入应为追加写，避免大文件全量重写。

### 6.2 可靠性
- 数据保存应使用"写临时文件 + 原子替换"策略，防止写入过程中断导致数据损坏。
- AI 模块初始化失败不得导致主程序崩溃；应降级为无 AI 的纯学习模式。
- MCP 服务器启动失败应记录日志，不影响主程序使用。
- 会话文件损坏时，应降级为空会话状态，而非崩溃。

### 6.3 安全性
- 配置中的 API Key 在界面上应脱敏显示（如 `●●●`）。
- 日志中应对敏感字段做掩码处理。
- MCP 服务器默认只监听本地地址。
- 路径操作应防止路径穿越（所有文件读写限定在数据目录内）。

### 6.4 可维护性
- 核心学习逻辑（SM-2、卡片管理）与 AI 模块应解耦，AI 模块通过明确接口访问卡片数据。
- 提供商调用应抽象为统一接口，新增提供商时只需实现该接口即可。
- 日志系统应支持依赖注入，方便测试时替换为 Mock 实现。

### 6.5 本地化与无障碍
- 所有面向用户的提示、标签、错误信息应支持本地化（中文为默认语言，可扩展多语言）。
- 界面应支持键盘全遍历（Tab 顺序合理）。
- 支持系统级减少动画偏好设置（若平台有该能力）。
- 支持高对比度模式（AAA 级对比度选项）。

---

## 7. 扩展性规划

以下功能在当前版本中尚未完整实现，但架构应预留扩展空间：

1. **语音输入/输出 (TTS/STT)**: 支持朗读题目/答案，语音输入消息。
2. **图片识别**: 允许卡片包含图片题目，支持 OCR 或直接展示。
3. **知识图谱**: 基于卡片标签和文本相似度构建关系图可视化。
4. **统计面板**: 学习曲线、每日复习量、记忆留存率等图表。
5. **社区/分享**: 卡片集导入导出、社区分享（需云端服务）。
6. **移动端同步**: 跨设备数据同步机制。
7. **Obsidian/笔记导入**: 从外部笔记软件批量导入知识卡片。

---

## 8. 文件与目录结构（逻辑分层）

> 以下仅为逻辑分层建议，重构时可按所选技术栈重新组织物理目录。

```
Papyrus/
├── Core/                       # 核心学习引擎（必须）
│   ├── CardRepository          # 卡片 CRUD、搜索、导入导出
│   ├── SM2Engine               # SM-2 算法实现
│   ├── BackupManager           # 自动/手动备份与恢复
│   └── SessionScheduler        # 到期卡片调度、学习状态机
├── AI/                         # AI 助手模块（可选）
│   ├── ProviderInterface       # 提供商抽象接口
│   ├── OpenAIProvider          # OpenAI 兼容实现
│   ├── OllamaProvider          # Ollama 本地实现
│   ├── AIModelManager          # 模型列表、切换、校验
│   ├── AIConfigManager         # 配置读写、验证
│   ├── ChatSessionManager      # 多会话管理、历史持久化
│   ├── AttachmentService       # 附件存储、格式转换
│   ├── CardTools               # 工具定义与执行
│   └── ToolParser              # 从 AI 回复中解析工具调用
├── MCP/                        # 外部服务接口（可选）
│   └── MCPServer               # HTTP 服务器、CORS、JSON-RPC 路由
├── Logging/                    # 日志系统
│   ├── Logger                  # 分级日志、多文件输出
│   ├── ActivityTracker         # 用户行为 JSONL 记录
│   └── EventTracker            # 结构化事件 JSONL 记录
└── Presentation/               # 界面层（由具体技术栈实现）
    ├── MainStudyView           # 主学习区（题目/答案状态机）
    ├── AISidebarView           # AI 助手面板
    ├── SettingsView            # 设置界面
    ├── LogViewerView           # 日志查看器
    └── StatusBar               # 状态栏
```

---

**文档结束**
